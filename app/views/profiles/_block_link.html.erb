<% if user.blocked_for? current_user %>
	<%= link_to 'Unblock', unblock_user_path(user), class: 'btn', method: :post %>
<% else %>
	<%= link_to 'Block', block_user_path(user), class: 'btn', method: :post %>
<% end %>
