<div class="page-header">
  <h2><%= t '.title', interlocutor: @conversation.interlocutor.name %></h2>
</div>
<p>
  <%= link_to t('.conversations'), conversations_path %>
  <%= render 'me/users_dates/unlock_button', users_date: @conversation.users_date %>
</p>
<div class="messages container-fluid">
  <%= render partial: 'message', collection: @conversation.messages %>
</div>
<%= render 'form', conversation: @conversation, message: @message %>
